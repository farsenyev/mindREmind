# 🧠 mindREmind — Telegram-бот для напоминаний и событий

`mindREmind` — это умный Telegram-бот, который помогает создавать напоминания, планировать события, приглашать участников и управлять списком задач простыми командами и кнопками.

Бот работает на **TypeScript + Telegraf**, использует **long polling**, поддерживает редактирование и удаление напоминаний/событий, а также удобный UI на инлайн-кнопках.

---

## ✨ Возможности

### 🔔 Напоминания

* `/remind <время> <текст>`
  Пример:

  ```
  /remind 15m купить молоко
  /remind 2h тренировка
  /remind 2025-12-10 19:30 позвонить
  ```
* Редактирование напоминания через кнопку ✏️
* Удаление напоминания через кнопку 🗑
* Просмотр напоминания через кнопку 🔍
* Автоматическое удаление после срабатывания

### 📅 События

* `/event <время> @юзер1 @юзер2 <описание>`
  Пример:

  ```
  /event 10m @ivanov созвон
  /event 1d @user1 @user2 встреча
  /event 2025-12-10 19:30 @anna ужин
  ```

* Приглашения автоматически отправляются участникам в личку

* Участники могут выбрать:

    * "✅ Приду"
    * "❌ Не смогу"

* Создатель тоже выбирает свой статус

* Удобный просмотр события с кнопкой **Открыть**

* Возможность редактировать (`/edit <id> ...`) и удалять событие

### 📋 Список

Команда `/list` отображает:

* ближайшие напоминания
* созданные события
* события, в которых вы участвуете

С удобными кнопками:

* 🔍 Открыть
* ✏️ Edit
* 🗑 Delete

---

## 🚀 Установка и запуск

### Требования

* Node.js ≥ 18
* pnpm
* Telegram Bot Token (через @BotFather)

### 1. Клонируйте проект

```bash
git clone https://github.com/yourname/mindREmind
cd mindREmind
cp .env.example .env
```

### 2. Установите зависимости

```bash
pnpm install
```

### 3. Заполните `.env`
Telegram -> @BotFather -> Create -> Copy Bot_Token

```
BOT_TOKEN=token_of_your_bot
```

### 4. Запустите в development-режиме

```bash
pnpm dev
```

### 5. Продакшн-сборка

```bash
pnpm build
pnpm start
```

## 🧩 Архитектура проекта

```
src/
│
├── commands/
│   ├── remind.ts         — команда /remind
│   ├── event.ts          — команда /event
│   ├──  list.ts           — команда /list
│   └──  help.ts           — команда /help
│
├── handlers/
│   └── callbackQuery.ts  — единый обработчик всех инлайн-кнопок
│
├── services/
│   ├── reminderService.ts — логика напоминаний
│   ├── eventService.ts    — логика событий, RSVP и уведомлений
│   └── userService.ts    — регистация пользователя
│
├── utils/
│   ├── parseReminder.ts   — парсинг времени
│   └── parseEventInput.ts — парсинг событий
│
├── types/
│   ├── event.ts   — типизация event
│   ├── reminder.ts   — типизация reminder
│   └── user.ts   — типизация user
│
└── bot.ts               — создание и запуск бота
```

## 🛠 Технологии

* **Node.js + TypeScript**
* **Telegraf** (Telegram Bot API)
* **pnpm**
* **dayjs** (форматирование дат)
* Архитектура с сервисами и единым обработчиком callback-запросов

---

## 🧑‍💻 TODO / Будущие улучшения

* Поддержка recurring-напоминаний (`every day at 9`)
* Автодобавление аватарок/дат в UI
* Календарный режим (инлайн-календарь)
* Хранение данных в базе (SQLite/Postgres)
* Веб-панель просмотра событий

---

## 📄 Лицензия

MIT — делайте что хотите, но со ссылкой на оригинал.

---

## ❤️ Автор

Создано с любовью и TypeScript
by **farsen** & **mindREmind**
